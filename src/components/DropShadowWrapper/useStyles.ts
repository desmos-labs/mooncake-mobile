import {makeStyle} from 'config/theme';

const useStyles = makeStyle(() => ({
  externalShadow: {
    alignSelf: 'stretch',
  },
  innerShadow: {
    alignSelf: 'stretch',
  },
}));

export default useStyles;
